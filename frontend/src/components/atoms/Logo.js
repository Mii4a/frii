import React from 'react';
import styled from 'styled-components';

const Logo = ({
    src
}) => (
  <StyledImg
    src={src}
  >
  </StyledImg>
);

const StyledImg　= styled.img`
`;

export default Logo;